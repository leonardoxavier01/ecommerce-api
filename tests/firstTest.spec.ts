describe("First Test", () => {
  describe("When x = 1 and y = 2", () => {
    it("returns 3", () => {
      const x = 1;
      const y = 2;

      const sum = x + y;
      expect(sum).toBe(3);
    });
  });
});
